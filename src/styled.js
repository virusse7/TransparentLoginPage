import styled from "styled-components";

export const Title = styled.h1`
    text-align: center;
`

export const DaysElement = styled.div`
    border: 1px solid ${({ theme }) => theme.color.gray};
    width: 80px;
`;

export const Container = styled.div`

`